---
import solidIcons from '../assets/iconNames';
import brandIcons from '../assets/iconNames.brands';

type IconName = typeof solidIcons[number] | typeof brandIcons[number];

interface Props {
  name: IconName;
}
const { name } = Astro.props;
---

<i class={`fas fa-${name}`}></i>
